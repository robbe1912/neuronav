# machine-local state (per-project generated state lives in the TARGET
# repo's .neuronav/: chroma store, base shards, graph.html bake)
.venv/
config.json
.codegraph/
.neuronav/
.tmp/

# python build artifacts
__pycache__/
*.pyc
.pytest_cache/
build/
dist/
*.egg-info/

# generated output
*.log

# generated screenshots (tests + manual QA — land in .tmp/, this is a
# repo-wide backstop against accidental commits anywhere)
*.png

tools/_*.py
.team_scratch/
